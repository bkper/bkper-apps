export interface PurchaseLogEntry {
    qt: string;
    uc: string;
    id: string;
    rt: string;
}

export interface LiquidationLogEntry {
    id: string;
    dt: string;
    qt: string;
    uc: string;
    rt: string;
}
