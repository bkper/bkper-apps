import { css } from 'lit';

export const confirmationDialogCSS = css`
    .content {
        display: grid;
        gap: var(--bkper-spacing-small);
    }

    p {
        margin: 0;
        font-size: 14px;
    }
`;
