import { css } from 'lit';

export const realizedResultsCSS = css`
    .intro h2 {
        margin: 0;
        font-size: var(--bkper-font-size-large);
    }

    .intro p {
        margin: 0;
        margin-top: var(--bkper-spacing-x-small);
        color: var(--bkper-color-grey-high);
    }

    .intro p span {
        font-weight: var(--bkper-font-weight-bold);
    }
`;
